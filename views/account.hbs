<h1>Accounts</h1>

{{#if errorOcurred}}
	<p>Internal server error, try again later!</p>
{{else}}
	{{#if account}}
		
		{{account.username}}
		{{account.password}}
		
	{{else}}
		<p>There are no accounts.</p>
	{{/if}}
	
{{/if}}